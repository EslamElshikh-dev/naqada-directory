import type { Metadata } from 'next';
import Link from 'next/link';
import { knowledgeHeritage, primaryKnowledgeContributor } from '@/lib/knowledge';
import styles from '../knowledge.module.css';
export const metadata:Metadata={title:'تراث ومعالم نقادة — الموسوعة',description:'المواقع والموضوعات التراثية والسياحية الواردة في مصادر موسوعة نقادة مع الإسناد للمؤلف.',alternates:{canonical:'/knowledge/heritage'}};
export default function HeritagePage(){const categories=[...new Set(knowledgeHeritage.map((item)=>item.category))];return <main id="main-content" className="page-main"><section className={styles.hero}><div className={`shell ${styles.heroGrid}`}><div><span className={styles.eyebrow}>التراث والمعالم</span><h1>نقادة التي <em>تحكي نفسها</em></h1><p>{knowledgeHeritage.length.toLocaleString('ar-EG')} موضوعًا موزعًا على {categories.length.toLocaleString('ar-EG')} محاور تراثية وسياحية.</p></div></div></section><div className={`shell ${styles.wrap}`}><Link className={styles.back} href="/knowledge">← موسوعة نقادة</Link><section className={styles.attribution}><div className={styles.seal}>أد</div><div><h2>المؤلف والمصدر</h2><p>{primaryKnowledgeContributor.attributionFull}</p></div></section><div className={styles.grid}>{knowledgeHeritage.map((item)=><article className={styles.card} key={item.id}><div className={styles.cardTop}><span className={styles.cardBadge}>{item.category}</span><small>#{item.order.toLocaleString('ar-EG')}</small></div><h2>{item.name}</h2><p>مدخل موثق من المصدر؛ السرد الموسع يعاد تحريره قبل النشر ولا يُنسخ من الكتاب حرفيًا.</p><Link href={`/knowledge/heritage/${encodeURIComponent(item.slug)}`}>عرض المدخل ←</Link></article>)}</div></div></main>;}