import type { Metadata } from 'next';
import Link from 'next/link';
import { knowledgePeople, primaryKnowledgeContributor } from '@/lib/knowledge';
import styles from '../knowledge.module.css';
export const metadata: Metadata = { title: 'أعلام نقادة — الموسوعة', description: 'سجل الأعلام والشخصيات الواردة في مصادر موسوعة نقادة، بوسوم المجال والمكان وإسناد واضح.', alternates: { canonical: '/knowledge/people' } };
export default function KnowledgePeoplePage(){ return <main id="main-content" className="page-main"><section className={styles.hero}><div className={`shell ${styles.heroGrid}`}><div><span className={styles.eyebrow}>أعلام وشخصيات</span><h1>وجوه من <em>ذاكرة نقادة</em></h1><p>{knowledgePeople.length.toLocaleString('ar-EG')} سجلًا مستخرجًا من المصدر، مع منع استنتاج النسب أو العلاقات من تشابه الأسماء.</p></div></div></section><div className={`shell ${styles.wrap}`}><Link className={styles.back} href="/knowledge">← موسوعة نقادة</Link><section className={styles.attribution}><div className={styles.seal}>أد</div><div><h2>إسناد السجل</h2><p>{primaryKnowledgeContributor.attributionFull}</p></div></section><div className={styles.grid}>{knowledgePeople.map((person) => <article className={styles.card} key={person.id}><div className={styles.cardTop}><span className={styles.cardBadge}>{person.group}</span></div><h2>{person.name}</h2><div className={styles.tags}>{person.professionTags.map((tag) => <span className={styles.tag} key={tag}>{tag}</span>)}{person.placeTags.map((tag) => <span className={styles.tag} key={tag}>{tag}</span>)}</div><Link href={`/knowledge/people/${encodeURIComponent(person.slug)}`}>عرض السجل ←</Link></article>)}</div></div></main>; }