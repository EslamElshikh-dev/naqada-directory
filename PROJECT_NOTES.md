# دليل نقادة — سجل التنفيذ

## النسخة الأولى

- ربط 244 سجل نشاط مصنفًا `ready` فقط.
- إدراج 64 موضعًا بعد استبعاد الحالات التي تحتاج تثبيتًا إداريًا.
- إدراج 29 سجل عائلة/فرع تاريخي بحالات النشر المسموح بها فقط.
- إنشاء سجل أولي لأعلام ومعالم أسمنت: 10 شخصيات و12 معلمًا.
- إضافة بحث عربي مطبع، فلاتر تصنيف وموضع، صفحات نتائج، تفاصيل، اتصال وخرائط.
- إضافة صفحات القرى، العائلات، الأعلام والمعالم، ومنهج التوثيق.

## قواعد ثابتة

- لا يُعرض أي سجل `manual_review` أو `official_review` أو درجة C في واجهة النشر.
- لا يُستنتج النسب من تشابه الأسماء.
- `internal_kin_group` ليس فرعًا عائليًا.
- بيانات النشاط لا تمثل اعتمادًا حكوميًا أو ضمانًا للخدمة.

## المرحلة التالية المقترحة

- صفحات SEO مستقلة للقرى والتصنيفات والسجلات.
- نموذج إضافة/تصحيح مدعوم بقاعدة بيانات ومراجعة إدارية.
- لوحة إدارة وسجل تغييرات وتحديثات دورية.
- أخبار نقادة الموثقة بعد تحديد المصادر وسياسة إعادة الصياغة.
